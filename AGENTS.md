# 仓库协作约定

## Git 管理

- 由代理主动做好本仓库的 Git 管理，包括检查状态、整理改动、验证和提交，无需用户反复提醒。
- 开始任务前检查当前分支、工作区和暂存区，识别已有改动，保留用户及其他任务的工作。
- 根据任务需要管理分支；新建分支默认使用 `codex/` 前缀，避免无关任务混入同一次提交。
- 完成一个可独立验证的改动后，主动检查差异、运行适用的验证，并创建职责单一、说明清楚的本地提交。
- 按明确路径或补丁块暂存，仅提交本任务的改动；不得直接把已有未跟踪文件或无关改动一并提交。
- 不把密钥、凭据、临时文件、缓存和无关构建产物加入版本控制。
- 保留已有提交和工作成果，不擅自丢弃改动、改写共享历史或强制推送；远程推送遵循用户已授权的范围。
- 完成后简要报告改动、验证结果、提交编号及仍未提交的相关事项。
